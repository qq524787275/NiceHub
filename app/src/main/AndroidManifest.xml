<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zhuzichu.nicehub">

    <dist:module dist:instant="true" />

    <application
        android:name="com.zhuzichu.nicehub.NiceApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name="com.zhuzichu.nicehub.launch.activity.LaunchActivity"
            android:theme="@style/LaunchTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.zhuzichu.nicehub.main.activity.MainActivity"
            android:launchMode="singleTop" />
        <activity
            android:name="com.zhuzichu.nicehub.login.activity.LoginActivity"
            android:launchMode="singleTop" />
    </application>

</manifest>